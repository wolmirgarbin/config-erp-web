server:
  port: 8081
  
ribbon:
  eureka:
    enabled: true

eureka:
  instance:
    preferIpAddress: true

feign:
  hystrix:
    enabled: true

logging:
  file: /home/wolmir/logs/component-application.log
